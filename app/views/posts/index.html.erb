<h2>
  <%= link_to 'アニメの追加',new_post_path, class:'header-menu' %>
  アニメ紹介</h2>
<ul>
  <% @posts.each do |post| %>
  <li>
    <%= link_to post.title, post_path(post)%>
    <%= link_to '[x]',
    post_path(post),
    method: :delete,
    class: 'command',
    data: {confirm: 'Sure?'} %>
    <%= link_to '[Edit]',edit_post_path(post), class: 'command' %>
    <!--postのid渡している-->
  </li>
  <%end%>
</ul>
<div class='bottom_message'>
  <p>※上の[x]押して消さないでください。</a>
</div>
<div>
  <p>アニメの追加<br>各アニメの名言を追加してね。</p>
</div>